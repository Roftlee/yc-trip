<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.yc.trip.api.business.dao.role.RoleDao">

    <!-- 对象与字段映射 -->
    <resultMap type="com.yc.trip.api.business.bo.role.RoleDomain" id="RoleMap">
        <!--  -->
        <result property="id" column="id" />
        <!-- 角色名称 -->
        <result property="roleName" column="role_name" />
        <!-- 创建人Id -->
        <result property="createdBy" column="created_by" />
        <!-- 是否删除0否1是(枚举:YesNoStatus[core]) -->
        <result property="isDelete" column="is_delete"  javaType="com.yc.trip.api.core.enums.YesNoStatus"  typeHandler="org.go.api.core.typehandler.UniversalEnumHandler" />
        <!-- 创建时间 -->
        <result property="createdTime" column="created_time" />
        <!-- 更新时间 -->
        <result property="updatedTime" column="updated_time" />
    </resultMap>

    <!-- 新增角色信息 -->
    <insert id="addRole" parameterType="com.yc.trip.api.business.bo.role.RoleDomain" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO `role`(
            role_name,
            created_by,
            is_delete)
        VALUES(
            #{roleName,jdbcType=VARCHAR},
            #{createdBy,jdbcType=NUMERIC},
            #{isDelete, javaType=com.yc.trip.api.core.enums.YesNoStatus, typeHandler=org.go.api.core.typehandler.UniversalEnumHandler, jdbcType=NUMERIC}
        )	
    </insert>
	
    <!-- 修改角色信息 -->
	<update id="updateRole" parameterType="com.yc.trip.api.business.bo.role.RoleDomain">
        UPDATE `role`
        <set>
            <if test="roleName!=null and roleName!='' ">
            role_name=#{roleName,jdbcType=VARCHAR},
            </if>
            <if test="createdBy!=null ">
            created_by=#{createdBy,jdbcType=NUMERIC},
            </if>
            <if test="isDelete!=null ">
            is_delete=#{isDelete, javaType=com.yc.trip.api.core.enums.YesNoStatus, typeHandler=org.go.api.core.typehandler.UniversalEnumHandler, jdbcType=NUMERIC},
            </if>
            <if test="createdTime!=null ">
            created_time=#{createdTime,jdbcType=TIMESTAMP},
            </if>
            <if test="updatedTime!=null ">
            updated_time=#{updatedTime,jdbcType=TIMESTAMP},
            </if>
        </set>
            WHERE id=#{id,jdbcType=NUMERIC}	
	</update>
	
    <!-- 查询角色信息 -->
	<select id="getRole" parameterType="com.yc.trip.api.business.bo.role.RoleDomain" resultMap="RoleMap">
        SELECT id,role_name,created_by,is_delete,
           created_time,updated_time
        FROM `role`
        <where>
            <if test="id!=null ">
                and id=#{id,jdbcType=NUMERIC}
            </if>
            <if test="roleName!=null and roleName!='' ">
                and role_name=#{roleName,jdbcType=VARCHAR}
            </if>
            <if test="createdBy!=null ">
                and created_by=#{createdBy,jdbcType=NUMERIC}
            </if>
            <if test="isDelete!=null ">
                and is_delete=#{isDelete, javaType=com.yc.trip.api.core.enums.YesNoStatus, typeHandler=org.go.api.core.typehandler.UniversalEnumHandler, jdbcType=NUMERIC}
            </if>
        </where>
        LIMIT 2	
	</select>
	
    <!-- 查询角色信息列表 -->
    <select id="queryRoleList" parameterType="com.yc.trip.api.business.bo.role.RoleDomain" resultMap="RoleMap">
            SELECT t.id,t.role_name,t.created_by,t.is_delete,
            t.created_time,t.updated_time
            FROM `role` t 
        <where>
            <if test="roleName!=null and roleName!='' ">
                AND role_name=#{roleName,jdbcType=VARCHAR}
            </if>
            <if test="createdBy!=null ">
                AND created_by=#{createdBy,jdbcType=NUMERIC}
            </if>
            <if test="isDelete!=null ">
                AND is_delete=#{isDelete, javaType=com.yc.trip.api.core.enums.YesNoStatus, typeHandler=org.go.api.core.typehandler.UniversalEnumHandler, jdbcType=NUMERIC}
            </if>
            <if test="keywords!=null and keywords!='' ">
                AND (
                     role_name LIKE CONCAT('%', #{keywords}, '%')
                 )
            </if>
            <if test="ids!=null and ids.size!=0 ">
                AND id IN
                    <foreach item="item" index="index" collection="ids" open="(" separator="," close=")">  
                        #{item}
                    </foreach>
            </if>
        </where>	
    </select>
	
</mapper>
