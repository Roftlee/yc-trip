<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.yc.trip.api.business.dao.brand.BrandDao">

    <!-- 对象与字段映射 -->
    <resultMap type="com.yc.trip.api.business.dto.brand.Brand" id="BrandMap">
        <!--  -->
        <result property="id" column="id" />
        <!-- 品牌名 -->
        <result property="name" column="name" />
        <!-- 品牌logo -->
        <result property="logo" column="logo" />
        <!-- 是否删除0否1是(枚举:YesNoStatus[core]) -->
        <result property="isDelete" column="is_delete"  javaType="com.yc.trip.api.core.enums.YesNoStatus"  typeHandler="org.go.api.core.typehandler.UniversalEnumHandler" />
        <!-- 创建人Id -->
        <result property="createdBy" column="created_by" />
        <!-- 创建时间 -->
        <result property="createdTime" column="created_time" />
        <!-- 更新时间 -->
        <result property="updatedTime" column="updated_time" />
    </resultMap>

    <!-- 品牌新增 -->
    <insert id="add" parameterType="com.yc.trip.api.business.dto.brand.Brand" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO `brand`(
            name,
            logo,
            <if test="isDelete!=null">is_delete,</if>
            created_by,
        )
        VALUES(
            #{name,jdbcType=VARCHAR},
            #{logo,jdbcType=VARCHAR},
            <if test="isDelete!=null">#{isDelete, javaType=com.yc.trip.api.core.enums.YesNoStatus, typeHandler=org.go.api.core.typehandler.UniversalEnumHandler, jdbcType=NUMERIC},</if>
            #{createdBy,jdbcType=NUMERIC},
        )	
    </insert>
	
    <!-- 品牌 修改-->
	<update id="update" parameterType="com.yc.trip.api.business.dto.brand.Brand">
        UPDATE `brand`
        <set>
            <if test="name!=null and name!='' ">
            name=#{name,jdbcType=VARCHAR},
            </if>
            <if test="logo!=null and logo!='' ">
            logo=#{logo,jdbcType=VARCHAR},
            </if>
            <if test="isDelete!=null ">
            is_delete=#{isDelete, javaType=com.yc.trip.api.core.enums.YesNoStatus, typeHandler=org.go.api.core.typehandler.UniversalEnumHandler, jdbcType=NUMERIC},
            </if>
            <if test="createdBy!=null ">
            created_by=#{createdBy,jdbcType=NUMERIC},
            </if>
            <if test="createdTime!=null ">
            created_time=#{createdTime,jdbcType=TIMESTAMP},
            </if>
            <if test="updatedTime!=null ">
            updated_time=#{updatedTime,jdbcType=TIMESTAMP},
            </if>
        </set>
            WHERE id=#{id,jdbcType=NUMERIC}	
	</update>
	
    <!-- 品牌 查询-->
	<select id="get" parameterType="com.yc.trip.api.business.query.brand.BrandQuery" resultMap="BrandMap">
        SELECT id,name,logo,is_delete,
           created_by,created_time,updated_time
        FROM `brand`
        <where>
            <if test="id!=null ">
                AND id=#{id,jdbcType=NUMERIC}
            </if>
            <if test="name!=null and name!='' ">
                AND name=#{name,jdbcType=VARCHAR}
            </if>
            <if test="logo!=null and logo!='' ">
                AND logo=#{logo,jdbcType=VARCHAR}
            </if>
            <if test="isDelete!=null ">
                AND is_delete=#{isDelete, javaType=com.yc.trip.api.core.enums.YesNoStatus, typeHandler=org.go.api.core.typehandler.UniversalEnumHandler, jdbcType=NUMERIC}
            </if>
            <if test="createdBy!=null ">
                AND created_by=#{createdBy,jdbcType=NUMERIC}
            </if>
        </where>
        LIMIT 2	
	</select>
	
    <!-- 品牌列表查询 -->
    <select id="queryList" parameterType="com.yc.trip.api.business.query.brand.BrandQuery" resultMap="BrandMap">
            SELECT t.id,t.name,t.logo,t.is_delete,
            t.created_by,t.created_time,t.updated_time
            FROM `brand` t 
        <where>
            <if test="name!=null and name!='' ">
                AND name=#{name,jdbcType=VARCHAR}
            </if>
            <if test="logo!=null and logo!='' ">
                AND logo=#{logo,jdbcType=VARCHAR}
            </if>
            <if test="isDelete!=null ">
                AND is_delete=#{isDelete, javaType=com.yc.trip.api.core.enums.YesNoStatus, typeHandler=org.go.api.core.typehandler.UniversalEnumHandler, jdbcType=NUMERIC}
            </if>
            <if test="createdBy!=null ">
                AND created_by=#{createdBy,jdbcType=NUMERIC}
            </if>
            <if test="keywords!=null and keywords!='' ">
                AND (
                     name LIKE CONCAT('%', #{keywords}, '%')
                     OR logo LIKE CONCAT('%', #{keywords}, '%')
                 )
            </if>
            <if test="ids!=null and ids.size!=0 ">
                AND id IN
                    <foreach item="item" index="index" collection="ids" open="(" separator="," close=")">  
                        #{item}
                    </foreach>
            </if>
        </where>	
    </select>
	
</mapper>
