<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.yc.trip.api.business.dao.user.UserCouponsDao">

    <!-- 对象与字段映射 -->
    <resultMap type="com.yc.trip.api.business.bo.user.UserCouponsDomain" id="UserCouponsMap">
        <!-- id -->
        <result property="id" column="id" />
        <!-- 用户Id -->
        <result property="userId" column="user_id" />
        <!-- 优惠券Id -->
        <result property="counponsId" column="counpons_id" />
        <!-- 是否使用0否1是(枚举:YesNoStatus[core]) -->
        <result property="isUsed" column="is_used"  javaType="com.yc.trip.api.core.enums.YesNoStatus"  typeHandler="org.go.api.core.typehandler.UniversalEnumHandler" />
        <!-- 创建时间 -->
        <result property="createdTime" column="created_time" />
        <!-- 更新时间 -->
        <result property="updatedTime" column="updated_time" />
    </resultMap>

    <!-- 新增用户优惠券 -->
    <insert id="addUserCoupons" parameterType="com.yc.trip.api.business.bo.user.UserCouponsDomain">
        INSERT INTO `user_coupons`(
            id,
            user_id,
            counpons_id,
            is_used)
        VALUES(
            #{id,jdbcType=NUMERIC},
            #{userId,jdbcType=NUMERIC},
            #{counponsId,jdbcType=NUMERIC},
            #{isUsed, javaType=com.yc.trip.api.core.enums.YesNoStatus, typeHandler=org.go.api.core.typehandler.UniversalEnumHandler, jdbcType=NUMERIC}
        )	
    </insert>
	
    <!-- 修改用户优惠券 -->
	<update id="updateUserCoupons" parameterType="com.yc.trip.api.business.bo.user.UserCouponsDomain">
        UPDATE `user_coupons`
        <set>
            <if test="userId!=null ">
            user_id=#{userId,jdbcType=NUMERIC},
            </if>
            <if test="counponsId!=null ">
            counpons_id=#{counponsId,jdbcType=NUMERIC},
            </if>
            <if test="isUsed!=null ">
            is_used=#{isUsed, javaType=com.yc.trip.api.core.enums.YesNoStatus, typeHandler=org.go.api.core.typehandler.UniversalEnumHandler, jdbcType=NUMERIC},
            </if>
            <if test="createdTime!=null ">
            created_time=#{createdTime,jdbcType=TIMESTAMP},
            </if>
            <if test="updatedTime!=null ">
            updated_time=#{updatedTime,jdbcType=TIMESTAMP},
            </if>
        </set>
            WHERE id=#{id,jdbcType=NUMERIC}	
	</update>
	
    <!-- 查询用户优惠券 -->
	<select id="getUserCoupons" parameterType="com.yc.trip.api.business.bo.user.UserCouponsDomain" resultMap="UserCouponsMap">
        SELECT id,user_id,counpons_id,is_used,
           created_time,updated_time
        FROM `user_coupons`
        <where>
            <if test="id!=null ">
                and id=#{id,jdbcType=NUMERIC}
            </if>
            <if test="userId!=null ">
                and user_id=#{userId,jdbcType=NUMERIC}
            </if>
            <if test="counponsId!=null ">
                and counpons_id=#{counponsId,jdbcType=NUMERIC}
            </if>
            <if test="isUsed!=null ">
                and is_used=#{isUsed, javaType=com.yc.trip.api.core.enums.YesNoStatus, typeHandler=org.go.api.core.typehandler.UniversalEnumHandler, jdbcType=NUMERIC}
            </if>
        </where>
        LIMIT 2	
	</select>
	
    <!-- 查询用户优惠券列表 -->
    <select id="queryUserCouponsList" parameterType="com.yc.trip.api.business.bo.user.UserCouponsDomain" resultMap="UserCouponsMap">
            SELECT t.id,t.user_id,t.counpons_id,t.is_used,
            t.created_time,t.updated_time
            FROM `user_coupons` t 
        <where>
            <if test="id!=null ">
                AND id=#{id,jdbcType=NUMERIC}
            </if>
            <if test="userId!=null ">
                AND user_id=#{userId,jdbcType=NUMERIC}
            </if>
            <if test="counponsId!=null ">
                AND counpons_id=#{counponsId,jdbcType=NUMERIC}
            </if>
            <if test="isUsed!=null ">
                AND is_used=#{isUsed, javaType=com.yc.trip.api.core.enums.YesNoStatus, typeHandler=org.go.api.core.typehandler.UniversalEnumHandler, jdbcType=NUMERIC}
            </if>
            <if test="keywords!=null and keywords!='' ">
                AND (
                 )
            </if>
            <if test="ids!=null and ids.size!=0 ">
                AND id IN
                    <foreach item="item" index="index" collection="ids" open="(" separator="," close=")">  
                        #{item}
                    </foreach>
            </if>
        </where>	
    </select>
	
</mapper>
