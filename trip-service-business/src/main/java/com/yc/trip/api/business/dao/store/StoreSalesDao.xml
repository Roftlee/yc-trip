<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.yc.trip.api.business.dao.store.StoreSalesDao">

    <!-- 对象与字段映射 -->
    <resultMap type="com.yc.trip.api.business.dto.store.StoreSales" id="StoreSalesMap">
        <!--  -->
        <result property="id" column="id" />
        <!-- 门店Id -->
        <result property="storeId" column="store_id" />
        <!-- 用户Id -->
        <result property="userId" column="user_id" />
        <!-- 邀请人Id -->
        <result property="inviteUserId" column="invite_user_id" />
        <!-- 销售等级1一级销售2二级销售3VIP销售(枚举:SalesLevel) -->
        <result property="salesLevel" column="sales_level"  javaType="com.yc.trip.api.business.enums.store.SalesLevel"  typeHandler="org.go.api.core.typehandler.UniversalEnumHandler" />
        <!-- 利润分成(百分比) -->
        <result property="commissionRate" column="commission_rate" />
        <!-- 是否删除0否1是(枚举:YesNoStatus[core]) -->
        <result property="isDelete" column="is_delete"  javaType="com.yc.trip.api.core.enums.YesNoStatus"  typeHandler="org.go.api.core.typehandler.UniversalEnumHandler" />
        <!-- 创建时间 -->
        <result property="createdTime" column="created_time" />
        <!-- 更新时间 -->
        <result property="updatedTime" column="updated_time" />
    </resultMap>

    <!-- 门店销售信息新增 -->
    <insert id="add" parameterType="com.yc.trip.api.business.dto.store.StoreSales" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO `store_sales`(
            store_id,
            user_id,
            invite_user_id,
            sales_level,
            commission_rate,
            <if test="isDelete!=null">is_delete</if>
        )
        VALUES(
            #{storeId,jdbcType=NUMERIC},
            #{userId,jdbcType=NUMERIC},
            #{inviteUserId,jdbcType=NUMERIC},
            #{salesLevel, javaType=com.yc.trip.api.business.enums.store.SalesLevel, typeHandler=org.go.api.core.typehandler.UniversalEnumHandler, jdbcType=NUMERIC},
            #{commissionRate,jdbcType=NUMERIC},
            <if test="isDelete!=null">#{isDelete, javaType=com.yc.trip.api.core.enums.YesNoStatus, typeHandler=org.go.api.core.typehandler.UniversalEnumHandler, jdbcType=NUMERIC}</if>
        )	
    </insert>
	
    <!-- 门店销售信息 修改-->
	<update id="update" parameterType="com.yc.trip.api.business.dto.store.StoreSales">
        UPDATE `store_sales`
        <set>
            <if test="storeId!=null ">
            store_id=#{storeId,jdbcType=NUMERIC},
            </if>
            <if test="userId!=null ">
            user_id=#{userId,jdbcType=NUMERIC},
            </if>
            <if test="inviteUserId!=null ">
            invite_user_id=#{inviteUserId,jdbcType=NUMERIC},
            </if>
            <if test="salesLevel!=null ">
            sales_level=#{salesLevel, javaType=com.yc.trip.api.business.enums.store.SalesLevel, typeHandler=org.go.api.core.typehandler.UniversalEnumHandler, jdbcType=NUMERIC},
            </if>
            <if test="commissionRate!=null ">
            commission_rate=#{commissionRate,jdbcType=NUMERIC},
            </if>
            <if test="isDelete!=null ">
            is_delete=#{isDelete, javaType=com.yc.trip.api.core.enums.YesNoStatus, typeHandler=org.go.api.core.typehandler.UniversalEnumHandler, jdbcType=NUMERIC},
            </if>
            <if test="createdTime!=null ">
            created_time=#{createdTime,jdbcType=TIMESTAMP},
            </if>
            <if test="updatedTime!=null ">
            updated_time=#{updatedTime,jdbcType=TIMESTAMP},
            </if>
        </set>
            WHERE id=#{id,jdbcType=NUMERIC}	
	</update>
	
    <!-- 门店销售信息 查询-->
	<select id="get" parameterType="com.yc.trip.api.business.query.store.StoreSalesQuery" resultMap="StoreSalesMap">
        SELECT id,store_id,user_id,invite_user_id,
           sales_level,commission_rate,is_delete,created_time,
           updated_time
        FROM `store_sales`
        <where>
            <if test="id!=null ">
                AND id=#{id,jdbcType=NUMERIC}
            </if>
            <if test="storeId!=null ">
                AND store_id=#{storeId,jdbcType=NUMERIC}
            </if>
            <if test="userId!=null ">
                AND user_id=#{userId,jdbcType=NUMERIC}
            </if>
            <if test="inviteUserId!=null ">
                AND invite_user_id=#{inviteUserId,jdbcType=NUMERIC}
            </if>
            <if test="salesLevel!=null ">
                AND sales_level=#{salesLevel, javaType=com.yc.trip.api.business.enums.store.SalesLevel, typeHandler=org.go.api.core.typehandler.UniversalEnumHandler, jdbcType=NUMERIC}
            </if>
            <if test="commissionRate!=null ">
                AND commission_rate=#{commissionRate,jdbcType=NUMERIC}
            </if>
            <if test="isDelete!=null ">
                AND is_delete=#{isDelete, javaType=com.yc.trip.api.core.enums.YesNoStatus, typeHandler=org.go.api.core.typehandler.UniversalEnumHandler, jdbcType=NUMERIC}
            </if>
        </where>
        LIMIT 2	
	</select>
	
    <!-- 门店销售信息列表查询 -->
    <select id="queryList" parameterType="com.yc.trip.api.business.query.store.StoreSalesQuery" resultMap="StoreSalesMap">
            SELECT t.id,t.store_id,t.user_id,t.invite_user_id,
            t.sales_level,t.commission_rate,t.is_delete,t.created_time,
            t.updated_time
            FROM `store_sales` t 
        <where>
            <if test="storeId!=null ">
                AND store_id=#{storeId,jdbcType=NUMERIC}
            </if>
            <if test="userId!=null ">
                AND user_id=#{userId,jdbcType=NUMERIC}
            </if>
            <if test="inviteUserId!=null ">
                AND invite_user_id=#{inviteUserId,jdbcType=NUMERIC}
            </if>
            <if test="salesLevel!=null ">
                AND sales_level=#{salesLevel, javaType=com.yc.trip.api.business.enums.store.SalesLevel, typeHandler=org.go.api.core.typehandler.UniversalEnumHandler, jdbcType=NUMERIC}
            </if>
            <if test="commissionRate!=null ">
                AND commission_rate=#{commissionRate,jdbcType=NUMERIC}
            </if>
            <if test="isDelete!=null ">
                AND is_delete=#{isDelete, javaType=com.yc.trip.api.core.enums.YesNoStatus, typeHandler=org.go.api.core.typehandler.UniversalEnumHandler, jdbcType=NUMERIC}
            </if>
            <if test="keywords!=null and keywords!='' ">
                AND (
                 )
            </if>
            <if test="ids!=null and ids.size!=0 ">
                AND id IN
                    <foreach item="item" index="index" collection="ids" open="(" separator="," close=")">  
                        #{item}
                    </foreach>
            </if>
        </where>	
    </select>
	
</mapper>
