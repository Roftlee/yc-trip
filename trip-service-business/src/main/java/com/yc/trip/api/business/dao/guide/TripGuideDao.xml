<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.yc.trip.api.business.dao.guide.TripGuideDao">

    <!-- 对象与字段映射 -->
    <resultMap type="com.yc.trip.api.business.bo.guide.TripGuideDomain" id="TripGuideMap">
        <!--  -->
        <result property="id" column="id" />
        <!-- 发布人用户Id -->
        <result property="userId" column="user_id" />
        <!-- 标题 -->
        <result property="title" column="title" />
        <!-- 内容 -->
        <result property="content" column="content" />
        <!-- 是否删除0否1是(枚举:YesNoStatus[core]) -->
        <result property="isDelete" column="is_delete"  javaType="com.yc.trip.api.core.enums.YesNoStatus"  typeHandler="org.go.api.core.typehandler.UniversalEnumHandler" />
        <!-- 创建时间 -->
        <result property="createdTime" column="created_time" />
        <!-- 更新时间 -->
        <result property="updatedTime" column="updated_time" />
    </resultMap>

    <!-- 新增旅游攻略信息 -->
    <insert id="addTripGuide" parameterType="com.yc.trip.api.business.bo.guide.TripGuideDomain" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO `trip_guide`(
            user_id,
            title,
            content,
            is_delete)
        VALUES(
            #{userId,jdbcType=NUMERIC},
            #{title,jdbcType=VARCHAR},
            #{content,jdbcType=VARCHAR},
            #{isDelete, javaType=com.yc.trip.api.core.enums.YesNoStatus, typeHandler=org.go.api.core.typehandler.UniversalEnumHandler, jdbcType=NUMERIC}
        )	
    </insert>
	
    <!-- 修改旅游攻略信息 -->
	<update id="updateTripGuide" parameterType="com.yc.trip.api.business.bo.guide.TripGuideDomain">
        UPDATE `trip_guide`
        <set>
            <if test="userId!=null ">
            user_id=#{userId,jdbcType=NUMERIC},
            </if>
            <if test="title!=null and title!='' ">
            title=#{title,jdbcType=VARCHAR},
            </if>
            <if test="content!=null ">
            content=#{content,jdbcType=VARCHAR},
            </if>
            <if test="isDelete!=null ">
            is_delete=#{isDelete, javaType=com.yc.trip.api.core.enums.YesNoStatus, typeHandler=org.go.api.core.typehandler.UniversalEnumHandler, jdbcType=NUMERIC},
            </if>
            <if test="createdTime!=null ">
            created_time=#{createdTime,jdbcType=TIMESTAMP},
            </if>
            <if test="updatedTime!=null ">
            updated_time=#{updatedTime,jdbcType=TIMESTAMP},
            </if>
        </set>
            WHERE id=#{id,jdbcType=NUMERIC}	
	</update>
	
    <!-- 查询旅游攻略信息 -->
	<select id="getTripGuide" parameterType="com.yc.trip.api.business.bo.guide.TripGuideDomain" resultMap="TripGuideMap">
        SELECT id,user_id,title,content,
           is_delete,created_time,updated_time
        FROM `trip_guide`
        <where>
            <if test="id!=null ">
                and id=#{id,jdbcType=NUMERIC}
            </if>
            <if test="userId!=null ">
                and user_id=#{userId,jdbcType=NUMERIC}
            </if>
            <if test="title!=null and title!='' ">
                and title=#{title,jdbcType=VARCHAR}
            </if>
            <if test="content!=null ">
                and content=#{content,jdbcType=VARCHAR}
            </if>
            <if test="isDelete!=null ">
                and is_delete=#{isDelete, javaType=com.yc.trip.api.core.enums.YesNoStatus, typeHandler=org.go.api.core.typehandler.UniversalEnumHandler, jdbcType=NUMERIC}
            </if>
        </where>
        LIMIT 2	
	</select>
	
    <!-- 查询旅游攻略信息列表 -->
    <select id="queryTripGuideList" parameterType="com.yc.trip.api.business.bo.guide.TripGuideDomain" resultMap="TripGuideMap">
            SELECT t.id,t.user_id,t.title,t.content,
            t.is_delete,t.created_time,t.updated_time
            FROM `trip_guide` t 
        <where>
            <if test="userId!=null ">
                AND user_id=#{userId,jdbcType=NUMERIC}
            </if>
            <if test="title!=null and title!='' ">
                AND title=#{title,jdbcType=VARCHAR}
            </if>
            <if test="content!=null ">
                AND content=#{content,jdbcType=VARCHAR}
            </if>
            <if test="isDelete!=null ">
                AND is_delete=#{isDelete, javaType=com.yc.trip.api.core.enums.YesNoStatus, typeHandler=org.go.api.core.typehandler.UniversalEnumHandler, jdbcType=NUMERIC}
            </if>
            <if test="keywords!=null and keywords!='' ">
                AND (
                     title LIKE CONCAT('%', #{keywords}, '%')
                 )
            </if>
            <if test="ids!=null and ids.size!=0 ">
                AND id IN
                    <foreach item="item" index="index" collection="ids" open="(" separator="," close=")">  
                        #{item}
                    </foreach>
            </if>
        </where>	
    </select>
	
</mapper>
